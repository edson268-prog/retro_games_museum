import 'package:flutter/material.dart';

class AccesoriosScreen extends StatelessWidget {
  const AccesoriosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Accesorios'));
  }
}